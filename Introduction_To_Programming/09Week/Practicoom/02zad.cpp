#include <iostream>

int main(){
    /*Инициализирайте целочислена променлива с произволна стойност, насочете указател към нея
    и изведете на екрана стойността на променливата, нейния адрес, стойността на указателя,
    адресът на указателя и стойността, пазена в адреса, към който указателят сочи.*/
    int a;
    std::cin >> a;
    int *apointer = &a;

    std::cout << a << '\n' << &a << '\n' << apointer << '\n' << &apointer << '\n' << *apointer;
}